module.exports = {
  name: 'help',
  alias: [ 'h' ],
  description: 'Displays this help',
  hidden: false,
  run: async (context) => {
    const { print } = context

    // console.log(context)
  }
}
